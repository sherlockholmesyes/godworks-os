[package]
name = "godworks-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Shared Godworks OS domain types for entities, authority, regions, and 2D spatial state."

[lib]
path = "src/lib.rs"
